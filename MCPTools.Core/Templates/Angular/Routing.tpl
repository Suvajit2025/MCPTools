import { Routes } from '@angular/router';
import { {{EntityName}}Component } from './{{Route}}.component';

export const {{Route}}Routes: Routes = [
  {
    path: '{{Route}}',
    component: {{EntityName}}Component
  }
];
